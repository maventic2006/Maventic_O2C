import { EventContext } from "kloBo/EventContext";
import { q_pms_annual_per_employees as q_pms_annual_per_employees_gen } from "o2c_v2/query_gen/q_pms_annual_per_employees";
export class q_pms_annual_per_employees extends q_pms_annual_per_employees_gen {
	// public async onBeforeQuery(oEvent: EventContext) {
	// 	let instance = oEvent.getObject();
	// 	let result = await instance.getResults();
	// 	console.log(result);
	// }
}
